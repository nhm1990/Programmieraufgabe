//"use strict";
const expect = require('chai').expect;
const { add, sub } = require('../src/calc.js');
/*
describe( "Suite1", () => {
    it('returns 3 when adding 1 + 2', () => {
        expect(add(1,2)).to.eq(3);
    })

    it('return 3 when adding 1 + 2', () => {
        expect(sub(5,4)).to.eq(1);
    })
});
*/